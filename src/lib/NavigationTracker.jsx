// Navigation tracker placeholder.
// User activity logging was tied to the Base44 backend and has been removed
// so the app runs standalone. Kept as a no-op component to preserve the
// existing component tree in App.jsx.
export default function NavigationTracker() {
    return null;
}
